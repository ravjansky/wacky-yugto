# Agent tooling — local skills, not part of the deliverable
.agent/
.claude/
CLAUDE.md

# Editor / IDE
.vscode/
.idea/
*.sublime-workspace
*.code-workspace

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Dependencies (none today — the site is buildless — but guard anyway)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logs / temp
*.log
npm-debug.log*
scratchpad/
tmp/
temp/
*.bak
*~

# Env
.env
.env.*
!.env.example

# Source assets that shouldn't ship (originals, PSDs, exports)
*.psd
*.ai
*.sketch
*.fig

# Accidental local design docs, not part of the site
/design-system.md
/wacky-yugto-portfolio.md
